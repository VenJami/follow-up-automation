import { redirect } from "next/navigation";

export default function ReviewLeslieShortcut() {
  // Use a stable token so the friendly link can enter the funnel.
  redirect("/review/review-leslie-sullivan-hometown-realty-group");
}

